---
# vars file for zabbix-agent (Alpine Linux)

# Contains the path to the dir without final '/' (was mixed before 2021-11-12)
zabbix_agent_conf_dir: /etc/zabbix/zabbix_agentd.conf.d
zabbix_agent_service: zabbix-agentd
